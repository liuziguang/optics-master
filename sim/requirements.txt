# 顶层依赖（其余为自动带入的传递依赖，见下方"完整冻结"）
# 创建环境：python -m venv .venv
# 安装：.\.venv\Scripts\python.exe -m pip install -r requirements.txt
#
# 注意：2026-09 实测，Python 3.14 的 wheel 在清华镜像上还没同步，
# 必须用 PyPI 官方源（默认源即可），不要加 -i 镜像参数。

numpy==2.5.3
matplotlib==3.11.2
scipy==1.18.1
pandas==3.0.6

# 完整冻结（含传递依赖）：
#   contourpy==1.4.0
#   cycler==0.12.1
#   fonttools==4.65.0
#   kiwisolver==1.5.1
#   packaging==26.3
#   pillow==12.3.0
#   pyparsing==3.3.3
#   python-dateutil==2.9.0.post0
#   six==1.17.0
#   tzdata==2026.4
